"""
Various small utilities to make training, experimenting, validating, etc. a bit easier
"""


def card_to_one_hot(name, idxs):
    """
    Args:
        name (str)  : card name to find
        idxs (list) : card_names object returned by 17Lands "gen_training_pairs()" function

    Return:
        one_hot (list) : one-hot representation of card 'name' given 'idxs'
    """
    print("Break")
    pass
